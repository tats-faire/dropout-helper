{
  "name": "dropout-addon",
  "version": "1.0.0",
  "description": "Dropout but it saves your volume and subtitle settings",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "build-dev": "node build.js -d",
    "watch": "node build.js -d -w"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.266",
    "esbuild": "^0.20.2",
    "web-ext": "^8.0.0"
  },
  "dependencies": {
    "cookie": "^0.6.0"
  }
}
